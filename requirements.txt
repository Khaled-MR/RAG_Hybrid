lancedb>=0.13.0
pyarrow>=15.0.0
FlagEmbedding>=1.2.10
ollama>=0.3.0
numpy>=1.24.0
# torch is pulled in by FlagEmbedding. If you need a specific CUDA build,
# install it manually first from https://pytorch.org/get-started/locally/
